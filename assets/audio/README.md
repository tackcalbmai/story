Drop optional personal audio here:

- `winter.mp3` — the real «Вечная зима» track or a deliberately cut excerpt.
- `love-voice.m4a` — a short original voice-note fragment containing the first «люблю».

The story works without either file. `app.js` falls back to its own procedural soundscape when these files are absent or blocked.
